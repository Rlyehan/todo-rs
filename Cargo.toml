[package]
name = "todo-rs"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tui = {version = "0.19", default-features = false, features = ['termion'] }
crossterm = "0.25"
termion = "2.0"
chrono = "0.4"
